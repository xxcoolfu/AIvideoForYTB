# AI 视频无限画布项目约定

## 项目定位
- 本项目是个人本地工具，不是面向公众的 SaaS。
- 第一目标是减少 AI 视频制作中的重复动作：分镜整理、素材关联、生成提交、结果保存。
- 用户不是开发人员，所以界面和反馈必须优先讲清楚“下一步能做什么”，不要暴露无意义的技术细节。

## 当前 MVP 边界
- 当前已经是多平台画布：Lovart 与即梦 CLI 共存，画布层统一管理平台、模型、模式、时长、比例；提交层按平台分别适配。
- Lovart 已接通图片和视频生成；即梦 CLI 已接通图片生成（`text2image`、`image2image`）和视频生成（`text2video`、`image2video`、`multimodal2video`）。
- 连续镜头工作流已接通：可排队等待上一镜视频完成，自动提取尾帧，自动连线并继续提交下一镜。
- 分镜导入、标签汇总、标签绑定、工作流自动建立都已作为本地状态主线能力，不再只是占位骨架。
- 分镜导入格式当前支持五列：`分镜号 | 衔接方式 | 图片提示词 | 视频提示词 | 模型推荐`；图片提示词和模型推荐允许为空。
- 不在画布内制作声音资产，只允许导入音频作为素材。

## 文件结构
- `server.js`：本地服务、项目目录读写、Lovart CLI 调用适配。
- `public/`：前端页面、样式和画布交互。
- `projects/`：默认项目保存目录，不提交生成内容。

## 数据约定
- 每个项目必须包含：
  - `project.json`
  - `canvas.json`
  - `shots.json`
  - `tags.json`
  - `jobs.json`
  - `input/`
  - `images/`
  - `videos/`
  - `thumbnails/`
  - `logs/`
- 画布资产以 `asset_id` 为真实身份，名称只是展示名。
- 分镜标签是项目级对象，同一个标签绑定一次后，所有引用该标签的分镜共享绑定。
- 标签从图片提示词和视频提示词两列共同提取。

## 修改规则
- 新功能先保持最小可用，不提前做复杂工作流。
- 任何 Lovart 调用失败都必须保存失败原因，不能静默吞掉。
- 任何即梦 CLI 调用失败也必须保存失败原因；如果是平台侧退回，前端状态至少要明确让用户知道“平台退回失败”。
- 不要覆盖用户素材或生成结果；同名文件必须追加数字后缀。
- 默认值可以预填，但不能锁死；用户必须还能手动改平台、模型、模式、分类和引用方式。
- 涉及视频尾帧、连续镜头接续、静帧提取时，优先使用本机 `ffmpeg/ffprobe`，不要退回浏览器猜帧，除非是手动预览时提取当前帧。
