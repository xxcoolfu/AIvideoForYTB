# AI 视频无限画布项目约定

## 项目定位
- 本项目是个人本地工具，不是面向公众的 SaaS。
- 第一目标是减少 AI 视频制作中的重复动作：分镜整理、素材关联、生成提交、结果保存。
- 用户不是开发人员，所以界面和反馈必须优先讲清楚“下一步能做什么”，不要暴露无意义的技术细节。

## 当前 MVP 边界
- 当前已经是多平台画布：Lovart 与即梦 CLI 共存，画布层统一管理平台、模型、模式、时长、比例；提交层按平台分别适配。
- Lovart 已接通图片和视频生成；即梦 CLI 已接通图片生成（`text2image`、`image2image`）和视频生成（`text2video`、`image2video`、`multimodal2video`）。
- 连续镜头工作流已接通：可排队等待上一镜视频完成，自动提取尾帧，自动连线并继续提交下一镜。
- 任务记录同时承担任务队列职责：手动提交和批量提交都应先生成任务记录，再由队列调度按平台限制自动提交。
- 分镜导入、标签汇总、标签绑定、工作流自动建立都已作为本地状态主线能力，不再只是占位骨架。
- 分镜导入格式当前支持五列：`分镜号 | 衔接方式 | 图片提示词 | 视频提示词 | 模型推荐`；图片提示词和模型推荐允许为空。
- 剧本导入/绑定是轻量项目素材能力：只保存项目剧本文本和选中段落到分镜的绑定，不自动拆全片、不自动批量改写。
- 提示词优化是文本建议能力：只处理当前分镜或节点的图片提示词、视频提示词、局部反馈和已绑定剧本段落，不看图、不看视频、不自动提交生成。
- 不在画布内制作声音资产，只允许导入音频作为素材。

## 文件结构
- `server.js`：本地服务、项目目录读写、Lovart CLI 调用适配。
- `public/`：前端页面、样式和画布交互。
- `prompt_policy.json`：工具级提示词优化规则快照，不跟具体项目绑定。
- `projects/`：默认项目保存目录，不提交生成内容。

## 数据约定
- 每个项目必须包含：
  - `project.json`
  - `canvas.json`
  - `shots.json`
  - `tags.json`
  - `jobs.json`
  - `prompt_context.json`
  - `script.json`
  - `input/`
  - `images/`
  - `videos/`
  - `thumbnails/`
  - `logs/`
- 画布资产以 `asset_id` 为真实身份，名称只是展示名。
- 分镜标签是项目级对象，同一个标签绑定一次后，所有引用该标签的分镜共享绑定。
- 标签从图片提示词和视频提示词两列共同提取。
- 标签引用必须完整匹配，`@里奥父亲` 不能同时命中 `@里奥`；别名匹配也不能在更长的人名或标签内部做子串命中。
- 提示词优化上下文只保存当前项目的覆盖项、确认经验和修改记录；工具级 policy 不重复写入项目目录。
- 剧本绑定保存在 `script.json`：剧本文本是项目级素材，分镜只通过 `shot_id -> segment_id` 引用选中的局部段落。
- 剧本绑定入口必须显示候选分镜内容预览或搜索结果，不能只给分镜号列表让用户凭记忆选择。
- `jobs.json` 是任务队列的真实状态源；不要只在前端保存“待提交”临时队列。
- 同一个生成节点如果已有未生成中的旧任务（等待、并发限制、失败、取消），重新入队时旧任务应从队列里移除，避免同一分镜卡多个无效任务。
- 已应用的提示词优化必须进入项目复盘页，至少保留用户反馈、原提示词、实际替换后的提示词、修改原因和风险提醒。
- 非 Kling 的提示词优化必须保留原提示词里的 `@` 标签引用；模型漏掉时由系统自动补回并提示风险。
- 提示词优化快捷反馈是项目级配置，保存在 `prompt_context.json`，默认值可恢复但不能锁死用户自定义。

## 修改规则
- 新功能先保持最小可用，不提前做复杂工作流。
- 任何 Lovart 调用失败都必须保存失败原因，不能静默吞掉。
- 任何即梦 CLI 调用失败也必须保存失败原因；如果是平台侧退回，前端状态至少要明确让用户知道“平台退回失败”。
- 即梦并发限制不需要用户手动解除；应等待已有即梦任务返回并下载结果后，再自动提交队列里的下一条。VIP 后缀模型可以绕开普通即梦并发限制。
- Lovart 队列登记可以批量，但提交必须单通道：同一时间只允许 1 条任务向 Lovart 上传/提交，等拿到平台返回的 `thread_id` 后再提交下一条；平台活跃任务仍最多 9 个，避免触发 10 个并发限制。
- Lovart 平台项目 ID 是本地项目级状态；同一个本地项目第一次提交时只创建 1 个 Lovart project，后续所有 Lovart 任务都必须复用这个 `lovart_project_id`。
- 不要覆盖用户素材或生成结果；同名文件必须追加数字后缀。
- 默认值可以预填，但不能锁死；用户必须还能手动改平台、模型、模式、分类和引用方式。
- 提示词优化结果必须先展示差异，用户确认后才写回；项目经验必须用户点击“记住”后才写入。
- 图片压缩应保留 `asset_id` 和所有绑定关系，不要求用户重新导入或重新绑定；压缩文件使用新文件名，原图不删除。
- 涉及视频尾帧、连续镜头接续、静帧提取时，优先使用本机 `ffmpeg/ffprobe`，不要退回浏览器猜帧，除非是手动预览时提取当前帧。
