# 画布内剧本与提示词优化计划

## 1. 目标

在 AI 视频无限画布中增加一个轻量的“剧本与提示词优化”能力。

v1 只做文本处理，不做看图、看视频诊断。用户在生成图片或视频不理想时，用自然语言描述问题，例如“人物不像”“动作太多”“镜头不稳”“画面不够电影感”，系统基于工具级规则、项目上下文和当前分镜信息，改写图片提示词、视频提示词或局部剧本文本。

默认使用 DeepSeek API，原因是这个场景主要是高频文本改写，DeepSeek 成本更低，足够先跑通闭环。后续如果需要让模型直接看图或看视频，再增加多模态模型作为第二阶段。

## 2. 核心原则

- 不影响现有 Lovart / 即梦提交链路。优化功能只改文本，提交仍走当前平台适配。
- 不自动改写所有分镜。v1 只优化用户当前选中的分镜或节点。
- 不自动应用结果。模型只给建议，用户确认后才写回画布。
- 不把完整 skill 每次塞给模型。skill 规则整理成工具级 policy，项目只记录覆盖项和经验。
- 不自动修改 skill 文件。skill 是规则来源，优化功能只是读取整理后的规则快照。

## 3. 上下文设计

上下文分成两层。

### 3.1 工具级 Prompt Policy

新增工具级文件：

```text
prompt_policy.json
```

它保存通用规则，不跟具体项目绑定。内容包括：

- Seedance 必须保留 `@` 资产锚点。
- Kling 不直接使用 `@`，需要改写成自然语言。
- Lovart 角色资产提交时必须保留 URL 规则。
- 即梦多图/单图提交继续使用本地已导入资产，不重新去远程 URL 拿图。
- 角色资产强调稳定身份，不写动作变体。
- 场景和道具资产保持无人称、无角色召唤。
- spoken lines 默认使用口语英文，除非用户明确要求其他语言。
- 连续镜头、尾帧接续、多图参考、快速制作等模式的边界规则。

这样做的原因：skill 规则是工具能力，不是某个项目的素材。放在工具级可以避免每个项目重复保存一份，也方便后续统一更新。

### 3.2 项目级 Prompt Context

新增项目级文件：

```text
prompt_context.json
```

它只保存当前项目自己的内容：

- `policy_version`：当前项目使用的工具级 policy 版本。
- `project_context`：项目风格、角色/场景/道具资产摘要、当前模型偏好。
- `learnings`：用户确认保存的项目经验。
- `revisions`：每次优化记录，包括原文、新文、用户反馈、是否已应用。

项目级上下文不是每个分镜一条独立对话，而是一个项目内持续积累的优化上下文。每次优化时仍然发送结构化分镜包，避免只依赖长聊天历史导致模型跑偏。

## 4. 用户流程

1. 用户选中一个分镜或生成节点。
2. 点击“提示词优化”。
3. 用户输入这次哪里不理想，例如“角色年龄显得太小”“视频动作太夸张”“不要增加新人物”。
4. 系统组装请求上下文：
   - 工具级 policy 摘要。
   - 当前项目上下文。
   - 当前分镜号、衔接方式、平台、模型、模式、时长、比例。
   - 原图片提示词、原视频提示词。
   - 绑定资产和 `@` 引用。
   - 用户本次反馈。
   - 最近确认过的项目经验。
   - 同一分镜最近几次修改摘要。
5. DeepSeek 返回优化建议。
6. 前端展示差异：
   - 原图片提示词。
   - 新图片提示词。
   - 原视频提示词。
   - 新视频提示词。
   - 修改原因。
   - 风险提醒。
   - 可记住的项目经验建议。
7. 用户选择：
   - 应用到当前分镜。
   - 只应用图片提示词。
   - 只应用视频提示词。
   - 放弃。
   - 将某条建议“记住为项目经验”。

## 5. 后端接口

新增最小接口：

```text
GET /api/prompt-policy
GET /api/prompt-context
POST /api/prompt-context
POST /api/prompt-optimize
```

### 5.1 GET /api/prompt-policy

读取工具级 `prompt_policy.json`。

### 5.2 GET /api/prompt-context

读取当前项目的 `prompt_context.json`。如果文件不存在，返回默认空上下文。

### 5.3 POST /api/prompt-context

保存用户确认的项目经验、项目覆盖项或优化记录。

### 5.4 POST /api/prompt-optimize

提交当前分镜包和用户反馈，调用 DeepSeek，返回结构化优化建议。

DeepSeek 返回格式固定为 JSON：

```json
{
  "revised_image_prompt": "",
  "revised_video_prompt": "",
  "change_summary": "",
  "project_learning_suggestions": [],
  "warnings": []
}
```

## 6. 本地设置

DeepSeek 配置继续放在本地设置中：

```text
projects/.local/settings.json
```

新增字段：

```json
{
  "deepseek": {
    "enabled": false,
    "apiKey": "",
    "baseUrl": "https://api.deepseek.com",
    "model": "deepseek-chat"
  }
}
```

密钥只存在本地设置，不进入代码，不提交仓库。

## 7. 前端交互

前端新增一个“提示词优化”面板或弹窗。

界面只展示用户需要决策的内容，不暴露无意义技术细节：

- 没有 API Key 时，提示“需要先配置 DeepSeek Key”，并引导到本地设置。
- 请求失败时，提示“优化失败，可以重试或手动修改”，并保留用户输入。
- 返回结果异常时，不覆盖原提示词，允许用户重试。
- 应用结果前必须展示差异。
- 项目经验建议默认不保存，必须用户点击“记住”。

## 8. v1 不做的事

- 不做图片或视频内容识别。
- 不自动判断生成失败原因。
- 不自动批量重写所有分镜。
- 不自动提交生成任务。
- 不自动修改 skill。
- 不替代现有分镜导入、标签绑定、Lovart、即梦提交流程。

## 9. 验证清单

- 无 DeepSeek API Key 时，提示清楚，不影响现有提交按钮。
- 单分镜优化后，用户可以只应用图片提示词或只应用视频提示词。
- 多个项目读取同一份 `prompt_policy.json`，项目内不重复保存整套 skill 规则。
- 项目经验只有在用户点击“记住”后才写入 `prompt_context.json`。
- DeepSeek 返回非 JSON 或字段缺失时，后端不崩溃，前端显示可重试提示。
- Lovart 角色资产仍保留 URL 规则。
- 即梦多图/单图提交仍使用本地已导入资产，不重新远程取图。
- 文生图、文生视频、单图生视频、多图生视频入口都不被优化功能影响。

## 10. 分阶段实施

### 第一阶段：保存规则和上下文

- 新增工具级 `prompt_policy.json`。
- 新增项目级 `prompt_context.json` 默认读写逻辑。
- 新增 DeepSeek 本地配置项。

### 第二阶段：跑通单分镜优化

- 新增 `POST /api/prompt-optimize`。
- 支持用户输入反馈。
- 返回新版图片提示词和视频提示词。
- 前端展示差异并允许手动应用。

### 第三阶段：项目经验沉淀

- 支持模型提出项目经验建议。
- 用户点击“记住”后写入项目上下文。
- 后续优化请求自动携带已确认经验。

### 第四阶段：增强能力

- 支持局部剧本改写。
- 支持按失败类型提供快捷反馈标签。
- 支持多模态模型接入，用于图片或视频诊断。

## 11. 默认决策

- v1 默认模型：DeepSeek `deepseek-chat`。
- v1 默认只处理文本，不看图、不看视频。
- 工具级规则文件只保存一份。
- 项目级上下文只保存项目自己的经验和修改记录。
- 优化结果必须用户确认后才写回。
- 当前计划先保存为文档，不立即开发功能本体。
